console.log('Olá mundo !')
alert('Olá mundo !') // para a execução do código no browser até clicar no 'OK', e no node aparecerá um erro "undefined"

const confirma = confirm('Confirma ?') // retorna um valor boolean

const nome = prompt('Digite o seu nome') // como se fosse um scanf de C, porém retorna uma string

// Para inverter um array: [varA, varB, varC] = [varB, varC, varA]

const num1 = 50

console.log(num1.toString(2))